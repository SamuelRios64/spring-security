package com.app.entities;

// Esta enum se usa para restringir los valores posibles
// del campo roleEnum en la entidad RoleEntity, asegurando que solo se asignen roles válidos.

public enum RoleEnum {
    ADMIN,
    USER,
    INVITED,
    DEVELOPER,
    CAJERO
}


